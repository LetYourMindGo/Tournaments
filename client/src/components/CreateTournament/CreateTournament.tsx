import React, { useState } from 'react';
import axios from 'axios';


const CreateTournament = (trigger: boolean) => {
  return (trigger) ? (
    <div>CreateTournament</div>
  ) : (<div></div>);
}

export default CreateTournament